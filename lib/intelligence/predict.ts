import type { Prediction, Evidence } from "./types";
export function predictMissRisk(input:{priority:"high"|"medium"|"low"; overdue:boolean; historicalMissRate:number; minutesUntilDeadline:number}, now=new Date()):Prediction { const priority=input.priority==="high"?0.2:input.priority==="medium"?0.1:0; const urgency=input.minutesUntilDeadline<=0?0.45:input.minutesUntilDeadline<180?0.25:0; const score=Math.min(1,Math.max(0,input.historicalMissRate)+priority+urgency+(input.overdue?0.2:0)); const confidence=Math.min(1,0.45+(input.historicalMissRate>0?0.35:0)); const evidence:Evidence[]=[{source:"historical_miss_rate",value:input.historicalMissRate,observedAt:now.toISOString()},{source:"urgency",value:urgency,observedAt:now.toISOString()}]; return {score,confidence,explanation:score>=0.65?"High miss risk from urgency and observed history.":score>=0.35?"Moderate miss risk; review timing before the deadline.":"Current evidence indicates stable completion risk.",evidence}; }
